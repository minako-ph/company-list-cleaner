{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "dev": "node --env-file=.env --experimental-strip-types --watch src/index.ts"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.6.0",
    "@hono/node-server": "^1.13.0",
    "@jp-opendata/gov-clients": "workspace:*",
    "@jp-opendata/normalize-jp": "workspace:*",
    "hono": "^4.6.0",
    "jose": "^6.2.3",
    "stripe": "^22.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
