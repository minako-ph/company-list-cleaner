# コードレビュー結果とP1実行指示（2026-07-08）— company-list-cleaner

保存先: `docs/review-2026-07-08.md` ／ 実施者: Claude Code（§4のゲートG1のみ柱2リポジトリでの作業）
検証済み: 2026-07-08時点のmainで `pnpm install → scope check → typecheck → test(18本) → build → scope check(dist)` 全グリーンを第三者環境で再現確認。

**総合評価**: P0の完成度は高い。CR-7はマニフェスト＋CIスクリプト（source/dist二重）＋vitestの三重で機械的に強制され、R3-1（UserProperties UUID単独）は旧3段フォールバックの撤去まで含め正確、法務4ページは出典文言逐語・特商法必須項目・R3-2/R3-7を反映済み、インボイスWeb-API調査（未決#1）は一次資料で解消済み（**1リクエスト最大10件・/numのみ使用・/diff//pointは不使用と明記**——CR整合の良い判断）。一方で**要件の大半（FR-2〜12・CR-1〜5の実装層＝バックエンド）は計画どおり未着手**であり、「完璧か」への正直な回答は「P0として完璧に近い／プロダクトとしては未完」。本書は微修正4件（§2）と柱2への波及2件（§3）を処理した上で、P1の実行計画（§4-5）に接続する。

---

## 1. 是認事項（レビュー済み・変更しないこと）

CR-7三重強制の構成／R3-1の実装とdecisions.mdの履歴管理（旧方式の無効化を履歴として残す運用）／build.mjsのGAS向けバンドル設計（IIFE＋グローバルスタブ＋rootDir=dist）／web4ページの出典文言・特商法（画面表示ベースの提供時期＝R3-7）／SYNC.mdの正直な現状注記と再取込み手順／`pnpm-workspace.yaml`の`allowBuilds`（**正しい**。§3-Aで実測根拠を記載）／invoice調査ノートのスコープ外エンドポイント明記。

## 2. 微修正（F3-1〜F3-4。Claude Codeが即時対応）

- **F3-1【中・事故防止】subtree誤配線の防止柵**: 現在の`packages/jp-corp-core/src/http.ts`は柱2の**F-1（キー漏洩）修正前**のコピーであり、エラーメッセージにキー付きURLが混入する（法人番号・インボイスAPIは`id=`クエリでアプリケーションIDを送るため、漏洩＝国税庁承認の取消リスクに直結）。対応: ①SYNC.md「現状の注意」に「**柱2のF-1修正（redactUrlForError）を含むHEADを再取込みするまで、backendから本ディレクトリのコードを参照しない**」を追記 ②AGENTS.mdに同旨を1行追加。コード自体は改変禁止ルールどおり触らない（再取込みで解消する）。
- **F3-2【小】** `backend/.env.example`に`ALERT_WEBHOOK_URL=`（N-4通知の受け口。未設定ならログのみ）を追記し、コメントで用途を1行説明。
- **F3-3【小・法務整合】** 特商法の「解約後も当該課金期間の満了までPro機能を利用できます」は**P1ライセンス実装への拘束**になる。`docs/decisions.md`に実装要件として1行記録: 「/license検証はStripeの`cancel_at_period_end`を尊重し、解約済みでも期間末まではvalidと判定する（特商法表記との一致が必須）」。§5のStep 5でこの仕様を実装する。
- **F3-4【小】** `docs/setup/gcp-oauth.md`等のTODO（運営者名・ドメイン）は人間タスクとしてNotionリストと重複管理になっている——decisions.mdの該当TODO行に「→ 人間タスク（Notion『人間のやる事リスト』へ移管済み）」を付記し、リポジトリ側のTODO巡回対象から外す。

## 3.【重要】柱2リポジトリへの波及事項（2件）

- **(A) 柱2レビューF-6の撤回（私＝レビュー側の誤り）**: 「`allowBuilds`は非正式キー→`onlyBuiltDependencies`へ置換」というF-6指示は**誤り**だった。実測（pnpm 11.10.0）: `pnpm approve-builds`は`allowBuilds`（マップ形式）を書き込み、`onlyBuiltDependencies`は**無視されIGNORED_BUILDSが出る**。対応: 柱2で F-6 を適用済みなら`allowBuilds: { esbuild: true }`へ差し戻し、`docs/review-2026-07-08.md`のF-6項に「撤回（2026-07-08実測: approve-buildsはallowBuildsを生成）」と追記、decisions.mdに1行。なおesbuildはoptionalDependenciesのバイナリで動くため**どちらでも機能影響はゼロ**（警告の正誤の問題）。
- **(B) R3-3ゲートの実行（P1の前提）**: `gov-clients/houjin`（Ver.4: /num /name、XML type=12・CSV Shift_JIS対応、名称検索mode×target、公式サンプルfixture＝本リポジトリのfixtures/houjinを柱2へ還元して使用可）と`gov-clients/gbizinfo`（v2: `https://api.info.gbiz.go.jp/hojin/v2/...`・ヘッダ`X-hojinInfo-api-token`・法人基本/補助金）を**柱2リポジトリ（正典）に実装**する。柱2の実データ主義（golden・drift検知・redact済みhttp層）に従う。完了後に本リポジトリでsubtree再取込み。

## 4. P1着手ゲート（この2つが揃うまでbackend実装を開始しない）

- **G1**: §3-(B)完了＝柱2mainに houjin/gbizinfo クライアント＋F-1修正が存在する。
- **G2**: SYNC.md手順で再取込み完了（履歴追記）。あわせて依存解決方式を確定: **柱2の`packages/schema-buffer`・`packages/normalize-jp`も同様に個別subtreeで取込み**、3ディレクトリを本リポジトリのpnpm workspaceへ登録して`workspace:*`を解決可能にする（decisions.mdの選択肢①を採用。npm公開への切替は将来の任意最適化）。SYNC.mdを3ディレクトリ対応に更新。

## 5. P1実行計画（ゲート通過後。詳細仕様は引継書§6-§10・追補R3-2/R3-5を正とし本書は差分のみ）

1. **backend骨格**: Hono＋TypeScript、Dockerfile、Cloud Run構成値（R3-5: `asia-northeast1` / `max-instances=1`＝横断直列の担保 / `min-instances=0`）。全公的API呼び出しは直列キュー（`RATE_RPS`既定1）を通す。**構造化ログはCR-5の3点のみ**（`{user_key, timestamp, registration_number}`）——ログスキーマのスナップショットテストで「社名・応答ボディがログに存在しない」ことを固定する。
2. **invoiceクライアント（本リポジトリ専用・`backend/src/clients/invoice.ts`）**: 公開関数は`lookupByRegistrationNumbers(numbers: string[])`**のみ**（CR-1/2の型レベル縛り）。10件ずつ分割・`type=21`(JSON)・`INVOICE_API_BASE`で検証環境切替・`INVOICE_ENABLED`既定false（縮退公開）。テストで①名称系の引数・関数が存在しない ②/diff //point を呼ぶコードが存在しない（CR-4） ③応答を永続化する経路が存在しない（CR-3）を明示的に検証。
3. **FR-3/4/6のルート**: `/resolve`（houjin名称検索→候補＋confidence）・`/enrich`（houjin基本3情報＋gbizinfo）。jp-corp-core（再取込み後）を使用。
4. **FR-9無料枠**: Firestoreに`{user_key, month, rows_used}`のみ（公表情報は保存しない）。月次リセットはドキュメントキー`{user_key}:{YYYY-MM}`方式で実装簡素化。
5. **FR-10ライセンス（R3-2フロー）**: Stripe webhook（**署名検証必須**）→JWT発行、`/license/claim`（session_id→検証→キー表示）、再表示（email→Stripe顧客照合）、`/license/verify`（署名＋購読状態、短TTLメモリキャッシュ、**F3-3: `cancel_at_period_end`は期間末までvalid**）。web/にthanksページと再表示フォームを追加（静的＋fetch）。
6. **GAS側P1**: サイドバー本実装（列マッピング・進捗・中断再開＝ステータス列が真実源）、`processBatch`実装（chunkRows 50行→backendへは**対象列の値のみ**送信＝N-3。行内の他セルを読まない）、FR-7新規列追記＋ステータス列、FR-11の個人事業主注記をUI/ヘルプに、CR-6出典文言をヘルプ常設、`getStableUserKey()`を無料枠問い合わせに接続。
7. **監視N-4**: Cloud Runエラー率アラート＋公的API連続失敗→`ALERT_WEBHOOK_URL`通知＋サイドバー障害表示（無言で失敗しない）。
8. **E2E受入**（要件書§8-4/8-5）: 「新規ユーザーが正規化＋法人番号付与を3分以内」「Checkout→成功ページでキー表示→解錠→再表示→解約」を手順書化し、実施は人間タスクとしてNotionリストへ追加依頼。

## 6. やらないこと（再掲・P1でも不変）

登録番号以外でインボイスAPIを照会する経路／応答の保存・キャッシュ・ログ／全件DL・/diff利用／スコープ4点目／LLM使用（v1）／税理士向け文言／`getActiveUser().getEmail()`への依存復活。

## 7. 完了条件

§2の4件＋§3-(A)反映で第1コミット（即時）。§3-(B)〜§5はゲート順に進め、各Stepで`pnpm typecheck && pnpm test && pnpm build`＋スコープ検査グリーン、CRテスト（§5-2の3本）グリーン、decisions.mdへ記録。P1完了の定義は引継書§11 P1のDoD（E2E通し）とする。
